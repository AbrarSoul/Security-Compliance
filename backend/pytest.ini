[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
markers =
    integration: tests requiring PostgreSQL
    e2e: full Sprint 2 workflow tests (requires PostgreSQL, migrations 016)
    sprint3: Sprint 3 module integration tests
    sprint3_e2e: Sprint 3 end-to-end workflow (requires PostgreSQL, migrations 024+)
